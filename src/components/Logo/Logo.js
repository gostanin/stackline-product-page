import React from "react";

import logoImg from "../../assets/logo.png";

const logo = (props) => {
    return (
        <div>
            <img src={logoImg} alt="Stackline" />
        </div>
    );
};

export default logo;
